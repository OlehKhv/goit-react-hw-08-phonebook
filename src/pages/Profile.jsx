import { UserProfile } from 'components/UserProfile/UserProfile';

const Profile = () => {
    return (
        <section>
            <UserProfile />
        </section>
    );
};

export default Profile;
